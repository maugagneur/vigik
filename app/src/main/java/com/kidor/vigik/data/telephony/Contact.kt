package com.kidor.vigik.data.telephony

/**
 * Representation of a phone contact.
 *
 * @param id   ID of the contact.
 * @param type Type of the contact.
 */
data class Contact(val id: Long, val type: Int)
